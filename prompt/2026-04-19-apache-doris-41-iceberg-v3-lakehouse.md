# OG image prompt — Apache Doris 4.1 on Iceberg V3

**Post:** [/posts/apache-doris-41-iceberg-v3-lakehouse/](../_posts/2026-04-19-apache-doris-41-iceberg-v3-lakehouse.md)
**Output path (after generation):** `/assets/img/posts/2026-04-19-apache-doris-41-iceberg-v3-lakehouse-og.png`
**Dimensions:** 1200 × 630 px (OG standard)

---

```
Hand-drawn whiteboard sketch style, technical illustration, clean line work with
slightly imperfect strokes, marker-pen feel. Color palette: primary blue #1C6FF0,
lighter sky blue #6FA8F5 as secondary, soft orange #FF8A3D for accent, warm beige
background #FDF8F0. Thin black outlines, minimal shading, flat hand-drawn icons.
Labels in clean sans-serif. No photo-realism, no 3D rendering. Landscape 1200×630.

Scene: a "one engine, full lifecycle" diagram — Apache Doris sitting above an
Iceberg V3 table, with four DML arrows pointing down at it.

Top third, center: a hand-drawn rounded rectangle labeled "Apache Doris 4.1"
in primary blue #1C6FF0 fill with a small lightning-bolt and "SQL" badge inside
— represents the single query engine.

From the Doris box, four thick blue arrows radiate downward (like a rake),
each labeled in marker above the arrow tip with a SQL operation:
- "SELECT"
- "UPDATE"
- "DELETE"
- "MERGE INTO"

Bottom half, center: a wide hand-drawn table/slab labeled "Iceberg V3 Table",
drawn as a horizontal stack of 3-4 "parquet file" rectangle icons (small
spreadsheet-grid doodles), with a thicker outline around the whole stack.

To the right of the table: a small puffin-bird doodle (cute, whiteboard-style)
holding a tiny bitmap tag in its beak — labeled "Deletion Vector" with a thin
arrow pointing into the file stack. Filled with soft orange #FF8A3D accent.

Below each file icon: two tiny hand-drawn system columns labeled "_row_id" and
"_SN" in a small font, with a dotted line indicating they're hidden columns.
Label this whole detail with a bracket and "Row Lineage" caption.

Top banner: bold hand-lettered title across the top in dark ink:
"Doris 4.1 × Iceberg V3"
Subtitle under the title in smaller marker lettering:
"Full lakehouse lifecycle, one SQL engine"

Overall composition: Doris at top, 4 arrows of operations flowing down into one
Iceberg V3 table, with V3-specific features (DV + Row Lineage) called out as
hand-drawn annotations on the table.
```

---

## Generation notes

- If the image model produces blurry labels (common at small font sizes), generate with the layout only and add labels in Figma / Keynote afterward.
- Recommended tools: Midjourney v6+, Flux.1 dev, DALL-E 3, Imagen 3.
